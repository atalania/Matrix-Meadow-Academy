{
  "name": "matrix-meadow-academy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "stem-assistant-bridge": "file:packages/stem-assistant-bridge"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "happy-dom": "^20.9.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  }
}
